/*
let myAnimals = ["dog","cat","horse","meerkat"]
let userEntry = prompt("Add the name of an animal to the array.")
let lowerCase = userEntry.toLowerCase()
myAnimals.push(lowerCase)
alert(`The last animal is a/an ${myAnimals[myAnimals.length - 1]}.`)
*/